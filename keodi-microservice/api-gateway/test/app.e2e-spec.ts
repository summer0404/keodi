/**
 * Placeholder e2e spec kept for backward compatibility.
 * Real integration tests are in auth.e2e-spec.ts and jwt-guard.e2e-spec.ts.
 */
describe('App bootstrap (e2e)', () => {
  it('e2e test suite is configured', () => {
    expect(true).toBe(true);
  });
});
