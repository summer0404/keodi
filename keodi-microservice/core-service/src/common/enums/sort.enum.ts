export enum SortBy {
  DISTANCE = 'distance',
  RATING = 'rating',
  NAME = 'name',
  CREATED_AT = 'createdAt',
}

export enum FriendSortBy {
  NAME = 'name',
  CREATED_AT = 'createdAt',
}

export enum SortOrder {
  ASC = 'asc',
  DESC = 'desc',
}
